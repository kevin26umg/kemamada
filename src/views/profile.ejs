<%- contentFor('body') %>
<div class="max-w-2xl mx-auto mt-8">
  <div class="bg-white shadow-md rounded-lg overflow-hidden">
    <div class="p-4 sm:p-6">
      <div class="flex items-center space-x-4">
        <img src="<%= user.profilePicture || '/uploads/default.png' %>" alt="Profile picture" class="w-24 h-24 rounded-full object-cover">
        <div>
          <h1 class="text-2xl font-bold text-gray-900"><%= user.username %></h1>
          <p class="text-gray-600"><%= user.email %></p>
        </div>
      </div>
      <div class="mt-4">
        <h2 class="text-xl font-semibold text-gray-900">Bio</h2>
        <p class="mt-2 text-gray-600"><%= user.bio || 'Biografía no disponible' %></p>
      </div>
      <div class="mt-6">
        <a href="/profile/edit" class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
          Editar perfil
        </a>
      </div>
    </div>
  </div>
</div>