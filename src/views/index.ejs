<%- contentFor('body') %>
<div class="flex items-start justify-center ">
    <div class="bg-white dark:bg-gray-700 rounded-lg p-10 shadow-xl text-center mt-20">
        <h1 class="text-5xl font-extrabold text-gray-800 dark:text-white mb-4">Bienvenido a Kemámada</h1>
        <p class="text-2xl text-gray-700 dark:text-gray-200 mb-8">Conéctate con tus amigos y comparte momentos.</p>
        <% if (!locals.user) { %>
            <div class="flex justify-center space-x-4">
                <a href="/auth/login" class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-6 rounded-lg transition duration-200">
                    Entrar
                </a>
                <a href="/auth/register" class="bg-green-600 hover:bg-green-700 text-white font-bold py-3 px-6 rounded-lg transition duration-200">
                    Registrarse
                </a>
            </div>
        <% } %>
    </div>
</div>